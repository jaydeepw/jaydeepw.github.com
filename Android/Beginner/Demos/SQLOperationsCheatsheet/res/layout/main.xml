<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <LinearLayout
	 	android:layout_width="match_parent"
	 	android:layout_height="wrap_content" >
	 	
        <EditText
            android:id="@+id/todo_item"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Add item here" />
        
        <Button
            android:id="@+id/todo_add"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_weight="4"
            android:text="Add" />
	    
    </LinearLayout>
    
    
    <!-- We should ideally use a listview for the purpose of showing
    todo items in the UI. But at the beginner level of students ListView
    adds some difficutly of understanding. -->
    <LinearLayout
        android:id="@+id/todo_list"
	    android:layout_width="fill_parent"
	    android:layout_height="fill_parent"
	    android:orientation="vertical">
    </LinearLayout>

</LinearLayout>